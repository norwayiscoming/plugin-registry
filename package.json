{
  "name": "openacp-plugin-registry",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "validate": "tsx scripts/validate.ts",
    "check-npm": "tsx scripts/check-npm.ts",
    "build": "tsx scripts/build-registry.ts",
    "update-versions": "tsx scripts/update-versions.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
